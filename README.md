# v0.7 Live Portfolio

Upload `portfolio.py` into the GitHub `ltp` folder.

Append the code in `MARKET_DATA_PATCH.txt` to the bottom of `ltp/market_data.py`.

Apply the changes in `APP_PATCH.txt` to the root `app.py`.

No Supabase update is required.

Quotes are delayed third-party data and may differ from E*TRADE.
